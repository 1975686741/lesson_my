const state = {
    all: []
}

const mutations = {
    setProducts (state) {
        // state.all = 
    }
}

export  default {
    namespaced: true, // 模块的名字  store.products.state
    state, 
    mutations
}