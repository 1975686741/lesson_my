/**
 * @func 判断 left 是否是 right 的实例
 * @param {*} left :对象
 * @param {*} right ：对象
 * @return boolean
 */
function myInstanceof(left, right) {
    // right 只要出现在left原型链的任何一栈都可以
    // return left instanceof right
}